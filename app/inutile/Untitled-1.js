/* const storeUser=async () => {
 
    dbRef.add({
      nom: nom,
      category: category,
     prenom:prenom,
      mail: mail,
    }).then((res) => {
      console.log("yar7am weldik")
    })
    .catch((err) => {
      console.error("Error found: ", err);
      this.setState({
        isLoading: false,
      });
    });
  
} */

 /*setnom(JSON.stringify(item.nom))*/